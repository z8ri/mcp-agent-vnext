server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # vue-router 用的是 history 模式，不存在的路径要落回 index.html
    # 让前端路由自己接管，不然刷新 /chat 会直接 404。
    location / {
        try_files $uri $uri/ /index.html;
    }
}
